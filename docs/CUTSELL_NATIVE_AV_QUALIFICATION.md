## Native AV qualification terminal failure and bounded timeline fix (2026-09-25)

Authorized batch tested SHA 5ff922a75963df9c62bd53bf44d98120bb3fc7e8 (engine 842f4c1). MOV run 36110633614 took 123.924s; Video00 run 36110633620 took 240.261s. Both compact results: ok=false, RuntimeError 'Required audiovisual Watch + Listen failed: ValueError: AV region outside source'. Both reached the Gemini response parser, so generation was attempted and returned JSON; neither reached editorial selection or render. No MP4 and no evidence of editing improvement. Workflow success reflects artifact preservation with continue-on-error, NOT engine success. Exact source verification passed before GPU; summary source_match=false is due to missing source hash in error-only compact output, not proof of a mismatched upload.

The failed version did not log rejected coordinates or response usage. Therefore the exact offending interval/confidence and exact billed Gemini cost cannot be recovered from these artifacts. The $0.20 authorization was an additional AV ceiling ($0.10 per run), not a fixed charge or total compute price. Failure can still incur provider and GPU usage. No third call or retry was launched.

Independent code inspection found a reproducible integration bug: prompt duration used the transcoded container duration while validation used original duration. Already-observed encoder padding can differ by <=0.3s (native AV preparation preflight bound). Fixed prompt to use original duration; only advisory regions beginning inside real source and ending inside verified encoder padding are intersected with the original end. Wholly outside, reversed, non-finite and beyond-prepared regions remain rejected. Added coordinate/duration/confidence diagnostics on failure and provider usage/reservation logging for future qualification. This addresses the reproducible padding defect; it is NOT proven to be the exact cause of both live failures because their rejected values were not preserved. 29 targeted tests passed, including padding intersection, wholly-padding rejection and existing source/context safeguards. No production change; native AV remains opt-in and unqualified. New paid qualification needs new authorization.
