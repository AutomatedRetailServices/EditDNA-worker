name: Benchmark 51 Build Status Probe
on:
  push:
    branches:
      - cutsell/mobile-v1-clean
    paths:
      - .github/workflows/benchmark51-build-status-probe.yml
permissions:
  contents: write
  actions: read
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: cutsell/mobile-v1-clean
          fetch-depth: 0
      - name: Capture Benchmark 51 build status
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --silent --show-error --url "https://api.github.com/repos/$GITHUB_REPOSITORY/actions/workflows/benchmark51-controlled-build.yml/runs?event=push&branch=cutsell%2Fmobile-v1-clean&per_page=5" --header "Authorization: Bearer $GH_TOKEN" --header "Accept: application/vnd.github+json" --output runs.json
          mkdir -p ops
          jq '.workflow_runs[0] | {id,run_number,status,conclusion,head_sha,created_at,updated_at}' runs.json > ops/benchmark51-build-status.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ops/benchmark51-build-status.json
          git commit -m "Record Benchmark 51 build status" || exit 0
          git pull --rebase origin cutsell/mobile-v1-clean
          git push origin HEAD:cutsell/mobile-v1-clean
# round-4 build refresh
