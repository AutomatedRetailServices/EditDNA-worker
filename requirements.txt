# HTTP + S3 + env
requests==2.32.3
boto3==1.35.36
python-dotenv==1.0.1

# Whisper (no torch needed, uses CTranslate2)
faster-whisper==1.0.3

# Arrays + images (for frame snapshots later)
numpy==1.26.4
Pillow==10.4.0

# OpenAI NEW CLIENT (for GPT-4o multimodal)
openai==1.51.0

# Progress/logging helper (optional)
tqdm==4.66.4
